create table products
(
    id          int auto_increment
        primary key,
    title       varchar(15) not null,
    subclass    varchar(40) null,
    price       float       not null,
    description text        null,
    path        text        null,
    type        varchar(6)  not null
);

